apiVersion: v1
kind: Service
metadata:
  name: {{ include "cfchart.fullname" . }}
  labels:
    {{- include "cfchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cfchart.selectorLabels" . | nindent 4 }}
